<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="160dp"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:padding="10dp"
    android:elevation="2dp"
    >

        <ImageView
            android:id="@+id/imageViewMeal"
            android:layout_width="match_parent"
            android:layout_height="170dp"
            android:scaleType="centerCrop"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:src="@color/accent"/>
        
        <TextView
            android:id="@+id/textViewMealName"
            android:textSize="15sp"
            android:textColor="@color/black"
            android:layout_margin="5dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toBottomOf="@id/imageViewMeal"
            app:layout_constraintStart_toStartOf="@id/imageViewMeal"
            app:layout_constraintEnd_toEndOf="@id/imageViewMeal"
            app:layout_constraintBottom_toBottomOf="parent"
            android:text="test"
            />


</androidx.constraintlayout.widget.ConstraintLayout>